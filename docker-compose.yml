services:
  postgres:
    container_name: postgres_db
    image: postgres:14
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - eventostec_db:/etc/data
    env_file:
      - .env.local

volumes:
  eventostec_db: